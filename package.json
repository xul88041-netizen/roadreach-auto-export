{
  "name": "roadreach-auto-export",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify.mjs",
    "check": "npm run verify && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
